name: "Sofle Firmware"

on:
  workflow_dispatch:
  push:
    branches:
    - main
    paths:
    - .github/workflows/sofle.yml
    - sofle/**

jobs:

  sofle-default-helios:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: medoix/build-qmk-fw@main
        with:
          keyboard: sofle
          keymap: rgb_default
          controller: helios
          local-keyboard-path: sofle
          output-dir: sofle
      - uses: sylvanld/action-storage@v1
        with:
          src: sofle
          dst: ./
          storage-branch: main

  sofle-vial-helios:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: medoix/build-qmk-fw@vial-qmk
        with:
          keyboard: sofle
          keymap: vial
          controller: helios
          local-keyboard-path: sofle
          output-dir: sofle
      - uses: sylvanld/action-storage@v1
        with:
          src: sofle
          dst: ./
          storage-branch: main

  sofle-vial-svlinky:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - uses: medoix/build-qmk-fw@main
        with:
          keyboard: sofle
          keymap: svlinky
          output-dir: sofle
          local-keyboard-path: sofle
      - uses: sylvanld/action-storage@v1
        with:
          src: sofle
          dst: ./
          storage-branch: main